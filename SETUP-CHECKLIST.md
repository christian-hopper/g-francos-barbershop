## New Client Setup

1. Duplicate this repo
2. Rename the GitHub repository
3. Update `src/data/business.ts`
4. Update `.env.production`:
   VITE_BASE_PATH=/new-repo-name/
5. Replace favicon in `public/`
6. Run:
   npm install
   npm run deploy
